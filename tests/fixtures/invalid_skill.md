This file is intentionally missing YAML frontmatter to test error handling.

It contains valid markdown content but no frontmatter block, which should
cause the skill parser to raise an error.

## Some Section

This is just plain text without the required metadata.
